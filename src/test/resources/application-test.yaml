spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://localhost:5433/jira-test}
    username: ${TEST_DB_USERNAME:jira}
    password: ${TEST_DB_PASSWORD:}